name: Build and Deploy Jigasi to ECR (RETIRED — every target below is gone)

# Superseded by ghcr-build.yml, which succeeded on the very same commits this failed
# on. Both targets here no longer exist:
#
#   * ECR_REPOSITORY `aiqlick-jigasi` is DELETED, so the push ended in
#       failed to push .../aiqlick-jigasi:latest: The repository with name
#       'aiqlick-jigasi' does not exist
#   * EC2_INSTANCE_ID i-0620d2e23695f5bfc is TERMINATED — Jitsi is not deployed
#     to instances any more. It runs on Kubernetes on the Swedish cluster.
#
# THE `push: branches: [main, master]` TRIGGER IS REMOVED, which is the only
# functional change — it is what made every push to master red while the GHCR build
# beside it went green. workflow_dispatch is left in place so the file stays a
# runnable record of the old pipeline rather than a deletion nobody can review.
#
# aiqlick-meeting/.github/workflows/ecr-deploy.yml was retired the same way, for the
# same reasons, and names this repo's ECR repository as deleted too.
on:
  workflow_dispatch:

env:
  AWS_REGION: eu-north-1
  ECR_REPOSITORY: aiqlick-jigasi
  EC2_INSTANCE_ID: i-0620d2e23695f5bfc
  AWS_ROLE_ARN: arn:aws:iam::842697652860:role/github-actions-role

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    permissions:
      id-token: write
      contents: read

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Configure AWS credentials via OIDC
        uses: aws-actions/configure-aws-credentials@v4
        with:
          role-to-assume: ${{ env.AWS_ROLE_ARN }}
          aws-region: ${{ env.AWS_REGION }}

      - name: Login to Amazon ECR
        id: login-ecr
        uses: aws-actions/amazon-ecr-login@v2

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Extract metadata
        id: meta
        run: |
          SHORT_SHA=$(git rev-parse --short HEAD)
          echo "sha=$SHORT_SHA" >> $GITHUB_OUTPUT
          echo "registry=${{ steps.login-ecr.outputs.registry }}" >> $GITHUB_OUTPUT

      - name: Build and push Docker image to ECR
        uses: docker/build-push-action@v5
        with:
          context: .
          file: ./Dockerfile
          push: true
          platforms: linux/amd64
          tags: |
            ${{ steps.login-ecr.outputs.registry }}/${{ env.ECR_REPOSITORY }}:latest
            ${{ steps.login-ecr.outputs.registry }}/${{ env.ECR_REPOSITORY }}:${{ steps.meta.outputs.sha }}
          cache-from: type=gha
          cache-to: type=gha,mode=max

      - name: Deploy to EC2 via SSM
        run: |
          echo "Deploying jigasi to EC2..."
          COMMAND_ID=$(aws ssm send-command \
            --instance-ids "${{ env.EC2_INSTANCE_ID }}" \
            --document-name "AWS-RunShellScript" \
            --parameters 'commands=["cd /opt/jitsi && aws ecr get-login-password --region eu-north-1 | docker login --username AWS --password-stdin ${{ steps.login-ecr.outputs.registry }} && docker compose pull jigasi && docker compose up -d --force-recreate jigasi"]' \
            --region ${{ env.AWS_REGION }} \
            --output text \
            --query 'Command.CommandId')

          echo "Waiting for deployment to complete..."
          aws ssm wait command-executed \
            --command-id "$COMMAND_ID" \
            --instance-id "${{ env.EC2_INSTANCE_ID }}" \
            --region ${{ env.AWS_REGION }} || true

          # Get command result
          aws ssm get-command-invocation \
            --command-id "$COMMAND_ID" \
            --instance-id "${{ env.EC2_INSTANCE_ID }}" \
            --region ${{ env.AWS_REGION }} \
            --query '[Status, StandardOutputContent]' \
            --output text

      - name: Deployment summary
        run: |
          echo "✅ Jigasi deployment completed!"
          echo "📍 Commit: ${{ steps.meta.outputs.sha }}"
          echo "🏷️  Image: ${{ steps.login-ecr.outputs.registry }}/${{ env.ECR_REPOSITORY }}:${{ steps.meta.outputs.sha }}"
          echo "🖥️  EC2: ${{ env.EC2_INSTANCE_ID }}"
